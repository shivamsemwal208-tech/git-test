export function RainfallChart({ values }: { values: number[] }) { const max = Math.max(...values); return <div className="flex h-44 items-end gap-2 border-b border-l border-white/10 px-3 pb-2 pt-4" aria-label="Simulated rainfall forecast chart">{values.map((value, index) => <div key={index} className="group flex flex-1 flex-col items-center justify-end gap-2"><span className="invisible rounded bg-black px-1 text-[10px] group-hover:visible">{value} mm</span><div style={{ height: `${Math.max(8, (value / max) * 105)}%` }} className="w-full rounded-t-md bg-gradient-to-t from-cyan-500/45 to-cyan-200" /><span className="text-[9px] text-slate-500">+{index + 1}h</span></div>)}</div> }
