import { PageHeader } from '../../components/layout/page-header'
import { RiskGauge } from '../risk/risk-gauge'
import { useCommand } from '../command-center/command-context'
export function FloodRiskPage() { const { command } = useCommand(); return <><PageHeader eyebrow="FLOOD-RISK INTELLIGENCE" title="Risk overview and factor context" description="This page displays a scenario fixture. It does not calculate or claim an ML prediction." /><div className="grid gap-5 lg:grid-cols-2"><RiskGauge probability={command.probability} riskLevel={command.riskLevel} summary={`${command.factors.join(' + ')} · DEMO / SIMULATION`} /><section className="border border-white/8 bg-white/[.03] p-5"><p className="text-[11px] font-bold tracking-[.15em] text-slate-400">RECOMMENDED ACTION</p><h2 className="mt-2 text-xl font-bold text-white">{command.riskLevel} scenario</h2><p className="mt-3 text-sm leading-7 text-slate-300">{command.action}</p><p className="mt-5 border-t border-white/8 pt-4 text-xs text-slate-500">No confidence value is displayed because no model is connected. Future backend risk responses will replace this demo fixture.</p></section></div><section className="mt-5 border border-white/8 bg-white/[.03] p-5"><p className="text-[11px] font-bold tracking-[.15em] text-slate-400">SIMULATION LOGIC EXPLANATION</p><div className="mt-4 grid gap-3 md:grid-cols-2">{command.factors.map((factor) => <article key={factor} className="border-l-2 border-rose-300/60 bg-black/10 p-4"><p className="font-semibold text-white">{factor}</p><p className="mt-1 text-sm text-slate-400">This factor is intentionally selected by the demo scenario. It is not generated by a real ML model.</p></article>)}</div></section></> }
