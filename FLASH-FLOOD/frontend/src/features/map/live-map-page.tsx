import { useState } from 'react'
import { CommandMap } from '../../components/map/command-map'
import { PageHeader } from '../../components/layout/page-header'
const layerNames = [['risk','Flood Risk'], ['rivers','Rivers'], ['earthquakes','Earthquakes'], ['places','Safe Places'], ['route','Evacuation Route']] as const
export function LiveMapPage() { const [layers, setLayers] = useState({ risk: true, rivers: true, earthquakes: false, places: true, route: false }); return <><PageHeader eyebrow="LIVE MAP" title="Interactive geographic intelligence" description="Pan and zoom the real OpenStreetMap basemap. Hazard, river, route, and marker overlays are illustrative demo layers." /><div className="grid gap-5 xl:grid-cols-[1fr_260px]"><CommandMap layers={layers} /><aside className="border border-white/8 bg-white/[.03] p-5"><p className="text-[11px] font-bold tracking-[.15em] text-slate-400">MAP LAYERS</p><div className="mt-4 space-y-3">{layerNames.map(([key, label]) => <label key={key} className="flex cursor-pointer items-center gap-3 text-sm text-slate-300"><input type="checkbox" checked={layers[key]} onChange={() => setLayers((current) => ({ ...current, [key]: !current[key] }))} className="accent-cyan-300" /> {label}</label>)}<label className="flex items-center gap-3 text-sm text-slate-500"><input type="checkbox" disabled /> Rainfall (future layer)</label><label className="flex items-center gap-3 text-sm text-slate-500"><input type="checkbox" disabled /> Satellite (future layer)</label><label className="flex items-center gap-3 text-sm text-slate-500"><input type="checkbox" disabled /> Landslide Risk (future layer)</label></div><p className="mt-6 border-t border-white/8 pt-4 text-xs leading-5 text-slate-500">Overlay geometry is illustrative and does not represent official hazard boundaries.</p></aside></div></> }
