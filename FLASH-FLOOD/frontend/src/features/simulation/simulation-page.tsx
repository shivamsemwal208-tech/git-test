import { Check } from 'lucide-react'
import { scenarioFixtures } from '../../data/demo-scenarios'
import { PageHeader } from '../../components/layout/page-header'
import { useCommand } from '../command-center/command-context'
export function SimulationPage() { const { scenarioId, setScenarioId, command } = useCommand(); return <><PageHeader eyebrow="SIMULATION CONTROL" title="Drive the command-center demo" description="Selecting a deterministic fixture updates risk, weather, terrain context, alerts, map layers, safe places, and emergency presentation across the frontend." /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{Object.values(scenarioFixtures).map((scenario) => <button onClick={() => setScenarioId(scenario.id)} key={scenario.id} className={`border p-5 text-left transition ${scenarioId === scenario.id ? 'border-cyan-200/60 bg-cyan-300/10' : 'border-white/8 bg-white/[.03] hover:bg-white/[.06]'}`}><div className="flex justify-between"><p className="font-bold text-white">{scenario.label}</p>{scenarioId === scenario.id && <Check size={17} className="text-cyan-200"/>}</div><p className="mt-4 text-3xl font-black text-white">{scenario.probability}%</p><p className="mt-1 text-xs font-bold tracking-[.13em] text-cyan-100">{scenario.riskLevel}</p><p className="mt-4 text-xs leading-5 text-slate-400">{scenario.factors.join(' · ')}</p></button>)}</div><section className="mt-6 border border-cyan-300/15 bg-cyan-300/[.05] p-6"><p className="text-[11px] font-bold tracking-[.15em] text-cyan-100">CURRENT SIMULATION</p><h2 className="mt-2 text-xl font-bold text-white">{command.label} · {command.location.name}</h2><p className="mt-2 text-sm text-slate-400">All values are fixed frontend fixtures. No live weather source, model, or backend is consulted.</p></section></> }
