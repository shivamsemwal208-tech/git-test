import { AlertTriangle } from 'lucide-react'
import { PageHeader } from '../../components/layout/page-header'
import { DataStatusBadge } from '../../components/status/data-status-badge'
import { useCommand } from '../command-center/command-context'
export function AlertsPage() { const { alerts } = useCommand(); return <><PageHeader eyebrow="ALERT CENTER" title="Simulation alerts and recommended actions" description="This is a frontend alert interface only. It does not send SMS, email, push notifications, or official warnings." />{alerts.length ? <div className="space-y-4">{alerts.map((alert) => <article key={alert.id} className="border border-white/8 bg-white/[.03] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold tracking-[.14em] text-rose-200">{alert.severity}</p><h2 className="mt-1 text-lg font-bold text-white">{alert.title}</h2></div><DataStatusBadge label="SIMULATION" /></div><div className="mt-4 grid gap-4 text-sm md:grid-cols-3"><p><span className="block text-xs text-slate-500">Location</span>{alert.location}</p><p><span className="block text-xs text-slate-500">Timestamp</span>{alert.timestamp}</p><p><span className="block text-xs text-slate-500">Reason</span>{alert.reason}</p></div><div className="mt-4 flex gap-2 border-t border-white/8 pt-4"><AlertTriangle size={16} className="text-amber-100"/><p className="text-sm text-slate-300">{alert.action}</p></div></article>)}</div> : <p className="border border-dashed border-white/15 p-8 text-center text-slate-400">No demo alerts are available.</p>}</> }
