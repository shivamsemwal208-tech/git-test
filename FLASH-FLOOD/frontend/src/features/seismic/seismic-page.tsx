import { RadioTower } from 'lucide-react'
import { PageHeader } from '../../components/layout/page-header'
import { useCommand } from '../command-center/command-context'
export function SeismicPage() { const { seismic } = useCommand(); const event = seismic[0]; return <><PageHeader eyebrow="SEISMIC MONITORING" title="Earthquake events and secondary-hazard awareness" description="FlashGuard does not predict earthquakes. This demo module represents detected/available event information and possible secondary-hazard context." /><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><section className="border border-amber-300/20 bg-amber-300/[.06] p-6"><RadioTower className="text-amber-100" /><p className="mt-5 text-[11px] font-bold tracking-[.15em] text-amber-100">RECENT EVENT · SIMULATION</p><h2 className="mt-2 text-3xl font-black text-white">M {event.magnitude}</h2><div className="mt-5 grid grid-cols-2 gap-4 text-sm"><p><span className="block text-slate-500">Depth</span>{event.depthKm} km</p><p><span className="block text-slate-500">Distance</span>{event.distanceKm} km</p><p><span className="block text-slate-500">Time</span>{event.time}</p><p><span className="block text-slate-500">Status</span>{event.status}</p></div></section><section className="border border-white/8 bg-white/[.03] p-6"><p className="text-[11px] font-bold tracking-[.15em] text-slate-400">SECONDARY HAZARD WATCH</p><h2 className="mt-2 text-xl font-bold text-white">Potential concerns</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[['Landslide', 'ELEVATED'], ['Rockfall', 'MONITORING'], ['River blockage', 'MONITORING'], ['Infrastructure disruption', 'DATA UNAVAILABLE'], ['Road blockage', 'DEMO ALERT']].map(([name,status]) => <div key={name} className="border-l-2 border-amber-300/50 bg-black/10 p-3"><p className="font-semibold text-white">{name}</p><p className="mt-1 text-xs text-amber-100">{status}</p><p className="mt-2 text-xs text-slate-500">Demo status; not a real detection.</p></div>)}</div><p className="mt-5 text-xs leading-5 text-slate-500">Earthquake prediction is not provided. Future seismic services will populate monitored events and secondary-hazard inputs.</p></section></div></> }
