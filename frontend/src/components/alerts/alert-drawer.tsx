import { X } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useCommand } from '../../features/command-center/command-context'
import { DataStatusBadge } from '../status/data-status-badge'
export function AlertDrawer({ onClose }: { onClose: () => void }) { const { alerts, dataSource } = useCommand(); return <div className="fixed inset-y-0 right-0 z-50 w-full max-w-md border-l border-white/10 bg-panel-2 p-5 shadow-2xl"><div className="flex items-center justify-between"><div><p className="text-xs font-bold tracking-[.15em] text-command-soft">ALERT CENTER</p><h2 className="mt-1 text-xl font-bold text-white">Simulation alerts</h2></div><button onClick={onClose} aria-label="Close alerts" className="rounded-lg border border-white/10 p-2"><X size={18} /></button></div><div className="mt-5 space-y-3">{alerts.map((alert) => <article key={alert.id} className="rounded-xl border border-white/8 bg-white/[.035] p-4"><div className="flex items-start justify-between gap-2"><span className="text-xs font-bold text-rose-200">{alert.severity}</span><DataStatusBadge label={dataSource === 'api' ? 'API · DEMO DATA' : 'DEMO / SIMULATION'} /></div><h3 className="mt-2 font-semibold text-white">{alert.title}</h3><p className="mt-1 text-xs leading-5 text-slate-400">{alert.reason}</p><p className="mt-3 text-xs text-slate-300">{alert.action}</p></article>)}</div><Link onClick={onClose} to="/alerts" className="mt-5 inline-block text-sm font-semibold text-command-soft">Open full alert center →</Link></div> }
